python3 -m venv env
cd env/Scripts && activate 
cd ../.. && pip install fastapi uvicorn

uvicorn main:app --reload --port=8000 --host=0.0.0.0
127.0.0.1:8000
127.0.0.1:8000/docs
ctrl+c

pip freeze > requirements.txt
deactivate

docker compose up


docker compose -f docker-compose.development.yaml up
docker compose -f docker-compose.staging.yaml up --build
docker compose -f docker-compose.production.yaml up --build -d


docker build ./ 
docker run -p 8000:8000 92855edcafbc

docker build -t andrewfowl/chatgpt-fastapi:1.0.0 .
docker login -u andrewfowl
docker push andrewfowl/chatgpt-fastapi:1.0.0

docker build -t andrewfowl/chatgpt-fastapi:1.0.1 .
docker login -u andrewfowl
docker push andrewfowl/chatgpt-fastapi:1.0.1